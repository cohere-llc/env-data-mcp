"""Developer scripts that ship with the package.

These modules are not part of the MCP server runtime; they are invoked
manually (or from CI) to maintain data files committed alongside the code.
"""
